FROM mcr.microsoft.com/dotnet/core/sdk:2.2 AS build
WORKDIR /app
COPY benchmark/*.csproj ./
RUN dotnet restore
COPY benchmark/. /app/
RUN dotnet publish -c Release -o out
ENTRYPOINT [ "dotnet","out/benchmark.dll" ]